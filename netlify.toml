[build]
  command = "npm install -g pnpm@10 && pnpm install --frozen-lockfile && pnpm run typecheck:libs || true && pnpm --filter @workspace/earnview run build"
  publish = "artifacts/earnview/dist/public"
  environment = { NODE_VERSION = "18" }

[context.production.environment]
  NODE_ENV = "production"
